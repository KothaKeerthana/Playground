#include<bits/stdc++.h>
using namespace std;
int main()
{
int n,p;
  cin>>n>>p;
  cout<<"Enter the value of a"<<endl<<"Enter the value of n"<<endl<<"The value of "<<n<<" power "<<p<<" is "<<pow(n,p);
}